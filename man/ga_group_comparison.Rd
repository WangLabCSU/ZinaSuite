% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis-ga-general.R
\name{ga_group_comparison}
\alias{ga_group_comparison}
\title{Group Comparison Analysis for General Analysis Module}
\usage{
ga_group_comparison(
  dataset = NULL,
  id = NULL,
  grp_df,
  samples = NULL,
  fun_type = c("betweenstats", "withinstats"),
  type = c("parametric", "nonparametric", "robust", "bayes"),
  pairwise.comparisons = TRUE,
  p.adjust.method = c("holm", "hochberg", "hommel", "bonferroni", "BH", "BY", "fdr",
    "none"),
  ggtheme = ggplot2::theme_bw(),
  ...
)
}
\arguments{
\item{dataset}{Optional character string specifying the dataset name}

\item{id}{Optional character string specifying the molecule identifier}

\item{grp_df}{Data frame with grouping information:
\itemize{
\item If dataset and id provided: 2-3 columns (sample, group, optional facet)
\item If no dataset/id: 3-4 columns (sample, value, group, optional facet)
}}

\item{samples}{Optional character vector of sample names to filter}

\item{fun_type}{Character string: "betweenstats" or "withinstats" (default: "betweenstats")}

\item{type}{Character string: "parametric", "nonparametric", "robust", or "bayes" (default: "parametric")}

\item{pairwise.comparisons}{Logical indicating whether to show pairwise comparisons (default: TRUE)}

\item{p.adjust.method}{Character string for p-value adjustment method (default: "fdr")}

\item{ggtheme}{ggplot2 theme object (default: ggplot2::theme_bw())}

\item{...}{Additional parameters passed to ggbetweenstats or ggwithinstats}
}
\value{
A ggplot object showing the group comparison
}
\description{
Perform group comparison analysis for a molecular identifier.
Supports both between-group and within-group comparisons.
}
\examples{
\dontrun{
# Create sample grouping data
grp_df <- data.frame(
  sample = c("S1", "S2", "S3", "S4", "S5", "S6"),
  group = c("A", "A", "B", "B", "C", "C"),
  value = c(1.2, 1.5, 2.3, 2.1, 3.1, 2.9)
)

# Direct comparison with value column
p <- ga_group_comparison(grp_df = grp_df)
print(p)
}
}

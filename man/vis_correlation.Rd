% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vis-correlation.R
\name{vis_correlation}
\alias{vis_correlation}
\title{Correlation Visualization}
\usage{
vis_correlation(
  x,
  y = NULL,
  type = c("scatter", "heatmap"),
  method = c("pearson", "spearman"),
  title = NULL,
  xlab = NULL,
  ylab = NULL,
  ...
)
}
\arguments{
\item{x}{First variable (numeric vector) or data frame for heatmap}

\item{y}{Second variable (numeric vector, optional for heatmap)}

\item{type}{Plot type: "scatter" or "heatmap"}

\item{method}{Correlation method for annotation}

\item{title}{Plot title}

\item{xlab}{X-axis label}

\item{ylab}{Y-axis label}

\item{...}{Additional arguments passed to ggplot}
}
\value{
ggplot object
}
\description{
Create scatter plots and heatmaps for correlation visualization.
}
\examples{
\dontrun{
# Query gene expression data
tp53_expr <- query_gene_expression("TP53", source = "tcga")
brca1_expr <- query_gene_expression("BRCA1", source = "tcga")

# Create scatter plot
p <- vis_correlation(tp53_expr, brca1_expr, type = "scatter")
print(p)

# Create correlation heatmap for multiple genes
genes <- c("TP53", "BRCA1", "EGFR", "KRAS")
expr_data <- sapply(genes, query_gene_expression, source = "tcga")
p <- vis_correlation(expr_data, type = "heatmap")
print(p)
}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis-ga-general.R
\name{ga_survival_analysis}
\alias{ga_survival_analysis}
\title{Survival Analysis for General Analysis Module}
\usage{
ga_survival_analysis(
  dataset = NULL,
  id = NULL,
  surv_df,
  samples = NULL,
  cutoff_mode = c("Auto", "Custom", "None"),
  cutpoint = c(50, 50),
  palette = "aaas",
  ...
)
}
\arguments{
\item{dataset}{Optional character string specifying the dataset name}

\item{id}{Optional character string specifying the molecule identifier}

\item{surv_df}{Data frame with survival information:
\itemize{
\item If dataset and id provided: 3 columns (sample, time, status)
\item If no dataset/id: 4 columns (sample, value, time, status)
}}

\item{samples}{Optional character vector of sample names to filter}

\item{cutoff_mode}{Character string: "Auto", "Custom", or "None" (default: "Auto")}

\item{cutpoint}{Numeric vector of cutpoints for Custom mode (default: c(50, 50))}

\item{palette}{Character string for color palette (default: "aaas")}

\item{...}{Additional parameters passed to survival plotting functions}
}
\value{
A ggplot object showing the survival analysis
}
\description{
Perform survival analysis for a molecular identifier.
Supports automatic or custom cutoff for grouping samples.
}
\examples{
\dontrun{
# Create sample survival data
surv_df <- data.frame(
  sample = c("S1", "S2", "S3", "S4", "S5", "S6"),
  value = c(1.2, 2.3, 3.1, 1.5, 2.1, 2.9),
  time = c(365, 200, 500, 180, 400, 300),
  status = c(1, 0, 1, 1, 0, 1)
)

# Direct survival analysis
p <- ga_survival_analysis(surv_df = surv_df)
print(p)

# With custom cutpoints
p <- ga_survival_analysis(
  surv_df = surv_df,
  cutoff_mode = "Custom",
  cutpoint = c(25, 75)
)
}
}

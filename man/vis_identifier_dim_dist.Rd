% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vis-dimension.R
\name{vis_identifier_dim_dist}
\alias{vis_identifier_dim_dist}
\title{Visualize Dimensionality Reduction}
\usage{
vis_identifier_dim_dist(
  data,
  method = c("pca", "tsne", "umap"),
  n_components = 2,
  color_by = NULL,
  shape_by = NULL,
  title = NULL,
  ...
)
}
\arguments{
\item{data}{Matrix or data frame with samples as rows}

\item{method}{Reduction method: "pca", "tsne", "umap"}

\item{n_components}{Number of components to compute}

\item{color_by}{Vector for coloring points (default: NULL)}

\item{shape_by}{Vector for shaping points (default: NULL)}

\item{title}{Plot title}

\item{...}{Additional parameters passed to reduction method}
}
\value{
ggplot object
}
\description{
Create scatter plot showing dimensionality reduction results (PCA, t-SNE, UMAP).
}
\examples{
\dontrun{
# Query multiple genes
genes <- c("TP53", "BRCA1", "EGFR", "MYC")
expr_list <- query_molecules(genes, n_workers = 4)

# Create expression matrix
expr_matrix <- do.call(rbind, lapply(expr_list, function(x) {
  as.numeric(x)
}))

# PCA plot
p <- vis_identifier_dim_dist(expr_matrix, method = "pca")
print(p)
}
}
